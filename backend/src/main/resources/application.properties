spring.application.name=matricula-web

# validate: Hibernate só valida o schema (não cria/altera tabelas). Schema fica a cargo do Flyway.
spring.jpa.hibernate.ddl-auto=validate

# Aplica migrations em src/main/resources/db/migration na subida da aplicação.
spring.flyway.enabled=true
